// Numbering items withins an array, each item is given a number automatically called an index.
// The index values start at zero (0)
var colors;
colors = ['White','black','custom'];

/* for this example;
0 - 'white'
1 - 'black'
2 - 'custom' */

/* accessing an item within an array */

var itemTHree;
itemTHree =  colors(2);

/* Number of items within the array */

var numColors;
numColors = colors.length;


